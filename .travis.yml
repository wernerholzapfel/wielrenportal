language: node_js
node_js:
- "10.10.0"
branches:
  only:
  - development
before_script:
- npm install -g @angular/cli
script:
- echo "Deploy!!"
- npm install
- ng build --prod
install:
- npm install -g firebase-tools
deploy:
  project: "wielerspel-aec4b"
  provider: firebase
  on:
    all_branches: true
  token:
    secure: $FIREBASE_TOKEN
